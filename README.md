# Peking University Family Canteen Simulator

本仓库用于提交课程项目材料。

- `pku_simulator/`：游戏项目代码、资源、音乐、运行入口和项目说明。
- 演示视频、作业报告、展示材料等文件可以放在仓库根目录，与 `pku_simulator/` 同级。

运行项目请进入代码目录：

```bash
cd pku_simulator
python -m pip install -e .
python run_qt.py
```
